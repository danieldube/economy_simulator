# Economy Simulator

This project simulates Germany’s wealth distribution, population structure, earnings, taxation, and government budgets using Monte-Carlo methods.

## Installation

```bash
python -m pip install --upgrade pip
python -m pip install -e ".[dev]"
```

## Running Tests

```bash
pytest
```

## Linting and Type Checking

```bash
ruff check .
ruff format --check .
mypy .
```
